#include <stdio.h>

int main(void)
{
    int x;
    int y;

    // printf("write the first number : ");
    scanf("%d", &x);
    // printf("write the second number : ");
    scanf("%d", &y);
    printf("SOMA = %d\n", x + y);
    return (0);
}